<div class="d-flex justify-content-between">
  <h2><%= @list.name %></h2>
  <p>
    <%= link_to lists_path, class:"btn btn-primary text-decoration-none" do %>
      <i class='fa-solid fa-clock-rotate-left'></i> Return to all lists
    <% end %>
  </p>
</div>

<p>
  <%= link_to new_list_bookmark_path(@list), class:"btn btn-primary text-decoration-none" do %>
    <i class='fa-solid fa-clock-rotate-left'></i> New lists bookmark
  <% end %>
</p>
